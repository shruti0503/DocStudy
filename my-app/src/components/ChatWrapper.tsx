

interface ChatWrapperProps {
    fileId: string
    isSubscribed: boolean
  }

const ChatWrapper =({
    fileId,
    isSubscribed,
  }: ChatWrapperProps)=>{

    return (
        <div>
            Chat Wrapper

        </div>
    )

}

export default ChatWrapper;